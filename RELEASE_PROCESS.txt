## tag the current head
git tag -a -m "test release" gluetools-0.1.0

## check what is the current version -- will be snapshot if there are commits since the last release.
gradle currentVersion

## move from snapshot to full release.
## this will increment the version, tag the commit and push the tag.
## fails if there are uncommitted changes.
gradle release